# Reports

Generated table outputs for the dual-sourcing benchmark suite land here.

The renderer expects per-instance JSON summaries under:

- `outputs/benchmarks/<run_tag>/instances/*.json`

and writes:

- `selected_policy_tables.md`
- `selected_policy_tables.tex`
