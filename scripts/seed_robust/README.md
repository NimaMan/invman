# Seed-Robust Audit Scripts

This folder contains cross-problem orchestration for seed-robust benchmark
audits.

- `run_full_budget_audits.sh` runs the long full-budget audit queue under the
  repository CPU-cap convention and writes logs under `outputs/`.

