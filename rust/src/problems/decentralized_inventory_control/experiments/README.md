# Experiments

This folder is the paper-facing benchmark home for `decentralized_inventory_control`.

Planned use:

- define reported decentralized-control instances
- compare CMA-ES-optimized learned policies against carried heuristics
- include the strongest exact or reduced reference comparator available

Current code anchors:

- heuristics in `heuristics/`
- exact reduced benchmark in `finite_horizon_dp.rs`
