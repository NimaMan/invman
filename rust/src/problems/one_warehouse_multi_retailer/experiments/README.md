# Experiments

This folder is the paper-facing benchmark home for `one_warehouse_multi_retailer`.

Planned use:

- define reported divergent-network instances
- compare CMA-ES-optimized learned policies against carried allocation heuristics
- include reduced exact comparators where tractable

Current code anchors:

- heuristics in `heuristics/`
- exact reduced benchmark in `finite_horizon_dp.rs`
