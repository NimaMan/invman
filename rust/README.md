# invman_rust

Rust extension module for high-throughput inventory-management rollouts.

Initial scope:
- validate the PyO3/maturin build path
- expose a soft-tree action helper
- expose a basic lost-sales constant-action rollout helper

Build into the project virtualenv with:

```bash
python ../scripts/build_rust_extension.py
```
