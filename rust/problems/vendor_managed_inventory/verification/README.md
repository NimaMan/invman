# Verification

The verification target for this family is a reduced single-retailer VMI instance with:

- explicit DC stock
- one-period retailer shipment pipeline
- deterministic DC replenishment
- lost-sales retailer demand

The executable assertions live in `rust/src/problems/vendor_managed_inventory/tests/verification.rs`.
