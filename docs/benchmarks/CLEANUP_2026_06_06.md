# Repo Cleanup — 2026-06-06 (F1 / F2 / F3)

Scope: cleanup pass per `PROPER_REPO_BUILD_PLAN.md` section (f). Conservative by mandate:
`git rm` (never plain `rm`) for tracked dead files; never touch `outputs/`, `paper/`, the
new `docs/benchmarks/` manifest/ledger/plan/cards, any `src/problems/*/env.rs|references.rs|
heuristics|exact solvers|BENCHMARK.md`, or anything with unmerged/uncommitted work; defer
anything uncertain.

## F1 — Dead scratch scripts importing `invman.policies.soft_tree`

**Result: NOTHING REMOVED. The F1 list in the build plan is STALE — all candidates were
already migrated off the dead module during the policy refactor.**

The dead module is `invman.policies.soft_tree` (note plural `policies`). The live Python
package is `invman/` with `invman/policy.py` (singular). There is **no** `invman/policies/`
directory in the main tree.

Verification performed on every plan-named F1 candidate:

- `python -c "import invman.policies.soft_tree"` → `ModuleNotFoundError: No module named
  'invman.policies'` (module confirmed absent).
- `git grep -n "invman\.policies" -- '*.py'` over tracked main-tree files returns **only
  comments/docstrings** that explicitly state the stale path was already fixed (e.g.
  "common.py now uses the …", "a module that was DELETED in the Python-cleanup",
  "NOT import the stale … module that the older scripts reference"). **Zero live imports.**
- Each plan-named candidate now imports `from invman.policy import Policy` (the migrated
  API, `backbone="soft_tree"`), not the dead module:

  | Candidate | Actual top-of-file import | Status |
  |---|---|---|
  | `scripts/perishable_inventory/run_paper_benchmark.py` | `from invman.es_mp import train` / `from common import …` | LIVE — `--help` loads OK |
  | `scripts/perishable_inventory/common.py` | `from invman.policy import Policy` | LIVE |
  | `scripts/procurement_removal_inventory/common.py` | `from invman.policy import Policy` | LIVE |
  | `scripts/procurement_removal_inventory/train_soft_tree_reference.py` | `from invman.es_mp import train` / `from common import …` | LIVE — `--help` loads OK |
  | `scripts/procurement_removal_inventory/validate_against_exact_dp.py` | `from common import …` | LIVE — `--help` loads OK |
  | `scripts/random_yield_inventory/common.py` | `from invman.policy import Policy` | LIVE |
  | `scripts/random_yield_inventory/train_soft_tree_reference.py` | `from invman.es_mp import train` / `from common import …` | LIVE — `--help` loads OK |

- Run from the repo root (where `invman` is on `sys.path`), all four runnable scripts execute
  `python <script> --help` successfully, fully loading their modules including the
  `from common import (build_soft_tree_model, evaluate_soft_tree_policy, …)` lines. (The only
  import error observed — `No module named 'invman'` — appears solely when `cd`-ing into a
  script subdir for an isolated import test; that is a `sys.path` artifact of the test, not a
  code defect, since these scripts are invoked from the repo root.)

Conclusion: removing any of them would delete LIVE, working scripts. No action taken for F1.

## F3 — `src/case_studies/hormuz_strait/`

**Result: DEFERRED / flagged in place. Not removed, not relocated, no data committed.**

Findings:

- **Tracked**: 44 files under `src/case_studies/hormuz_strait/` are tracked in git
  (`bindings.rs`, `flownet/`, `scenarios/`, `references.rs`, READMEs, result JSON/CSV/MD, …).
- **Referenced by crate/benchmark code** (so it is NOT out-of-benchmark scratch):
  - `src/lib.rs:1` → `pub mod case_studies;`
  - `src/lib.rs:22` → `case_studies::hormuz_strait::bindings::register_py(m)?;`
  - `src/case_studies/mod.rs:1` → `pub mod hormuz_strait;`
  It is compiled into the crate and registers a Python binding.
- **Untracked data dirs** present on disk (left in place, NOT added to any commit):
  `data/` (7.4M), `history/data/` (1.3M), `maritime_traffic/data/` (36M). `.gitignore`
  carries `data/` plus explicit `!src/case_studies/hormuz_strait/**/data/**` un-ignore rules;
  `git check-ignore` confirms those negation rules match, i.e. the data dirs are currently
  shown as untracked `??` only because they were never `git add`ed. They were deliberately
  excluded from this cleanup commit (scoped add) so ~45MB of data is not swept in.

Disposition rationale: the guardrails say to gitignore-and-leave UNTRACKED out-of-benchmark
trees, and to RELOCATE only TRACKED-AND-UNREFERENCED trees. hormuz_strait is TRACKED **and
referenced and compiled into the crate**, so neither branch applies mechanically. Relocating
it would require editing `src/lib.rs` + `src/case_studies/mod.rs` and a Rust rebuild — that is
a code change, not a safe mechanical move, so per the "if unsure, DEFER" rule it is deferred.
Recommended follow-up (manual, build-aware): keep it as an explicitly-labeled out-of-benchmark
case study under `src/case_studies/` (it is already namespaced there and documented by
`src/case_studies/README.md`), OR extract to a sibling repo together with its `lib.rs`
registration and a rebuild. No data deleted.

## F2 — Worktrees

**Result: `git worktree prune` run (safe no-op — nothing stale). ALL 9 worktree removals
DEFERRED.** Every worktree is LOCKED and held by a LIVE `claude` process (pid 3842443,
elapsed 2d+), and 8 of 9 carry unique unmerged commits; the 9th holds the MEMORY-flagged
incomplete ameliorating env-rewrite as uncommitted changes. None qualified for removal.

`git worktree prune -v` → no output, exit 0 (all 9 worktree dirs exist; no admin entries to
clear). Worktree count unchanged at 10 (main + 9).

Per-worktree evidence (`git status --short`; `git log <branch> --not main dev --oneline`):

| Worktree (branch `worktree-*`) | Dir | Locked / live pid | Uncommitted | Unique commits vs main+dev | Verdict |
|---|---|---|---|---|---|
| `agent-a18fd4b5f6ed17b6f` | yes | locked, pid 3842443 | 0 | 1 — `7a01af4 Add faithful, literature-verified ameliorating_inventory env (Pahr & Grunow 2025)` | DEFER (unmerged commit + live lock) |
| `agent-a6a66084c44547a69` | yes | locked, pid 3842443 | 0 | 1 — `f9b6814 Add faithful Maggiar & Sadighian (2017) joint pricing/inventory/removal env` | DEFER (unmerged + live lock) |
| `agent-acfc73f089cba6b80` | yes | locked, pid 3842443 | 10 (the ameliorating env-rewrite: `average_profit_blending_env.rs`, `issuance_blending_lp.rs`, `perfect_information_lp.rs`, `lp_dataset_loader.rs`, `practical/datasets/`, + `Cargo.toml/lock`, `mod.rs`) | 0 | DEFER — MEMORY-flagged INCOMPLETE/untested work to preserve + live lock |
| `wf_0591d276-78c-7` | yes | locked, pid 3842443 | 0 | 17 (e.g. `4c51d95`, `01c657a`, `dc56aa4`, `42a06ee`, …) | DEFER (large unmerged set + live lock) |
| `wf_c1e2aa4c-a0c-1` | yes | locked, pid 3842443 | 0 | 1 — `24788ab Make nonstationary_lot_sizing verification status honest (literature_verified=false)` | DEFER (unmerged + live lock) |
| `wf_c1e2aa4c-a0c-2` | yes | locked, pid 3842443 | 0 | 10 (e.g. `4a219c9`, `412033e`, `fae9b08`, `2eab463`, `d33b546`, `2d2e35e`, `7248505`, `997a4f4`, …) | DEFER (large unmerged set + live lock) |
| `wf_c1e2aa4c-a0c-3` | yes | locked, pid 3842443 | 0 | 1 — `af4335c Make divergent special-delivery honest: no published anchor under faithful gijs_2022 MDP` | DEFER (unmerged + live lock) |
| `wf_c1e2aa4c-a0c-4` | yes | locked, pid 3842443 | 0 | 1 — `9c7975f spare_parts_inventory: honest verification scope (env.rs not literature-verified)` | DEFER (unmerged + live lock) |
| `wf_c1e2aa4c-a0c-5` | yes | locked, pid 3842443 | 0 | 1 — `34f80a1 vendor_managed_inventory: make literature-verified status honest` | DEFER (unmerged + live lock) |

Notes:
- `f9b6814` (procurement_removal / Maggiar & Sadighian env) is **NOT** an ancestor of HEAD,
  main, or dev, and no equivalent cherry-pick exists in HEAD — contrary to the build-plan F3
  hint that it was "already cherry-picked". The unique work is still only in its worktree.
  DEFER preserves it.
- Because all worktrees are locked by a running agent, even the one clean+zero-unique-commit
  candidate (`agent-acfc73f089cba6b80`) is unsafe to remove — and it additionally holds the
  incomplete ameliorating rewrite. **All worktree removals deferred — the safe, expected
  outcome.**

## Net actions in this cleanup commit
- `git worktree prune` (no-op; recorded for the audit trail).
- Added this summary file: `docs/benchmarks/CLEANUP_2026_06_06.md`.
- No `git rm`, no `git mv`, no data added. F1 found nothing dead; F3 deferred; F2 all deferred.
